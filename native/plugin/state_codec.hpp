#pragma once

// Historical include path retained for CLAP tests while the byte image and
// stream adapter live behind separate neutral and CLAP-only interfaces.
#include "clap_state_stream.hpp"
#include "m3/state_image.hpp"
