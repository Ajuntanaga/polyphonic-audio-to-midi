# The pinned SDK is retained byte-for-byte, including upstream whitespace and
# short Setext-heading lines that resemble Git's default conflict markers.
third_party/vst3sdk/** -whitespace conflict-marker-size=32
